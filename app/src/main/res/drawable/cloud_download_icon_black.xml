<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Black solid circle background -->
    <item android:width="39dp" android:height="39dp">
        <shape android:shape="oval">
            <solid android:color="@color/black" /> <!-- black background color -->
        </shape>
    </item>

    <!-- Your vector icon -->
    <item
        android:width="24dp"
        android:height="24dp"
        android:drawable="@drawable/cloud_upload_white"
        android:gravity="center"
        />
</layer-list>