<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <include layout="@layout/topbar" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/action_bar"
        android:orientation="vertical">

        <!-- 标题和按钮区域 -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp">

            <!-- Proof Submission Button (Square, Left) -->
            <TextView
                android:id="@+id/proofAndNftButton"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:layout_alignParentStart="true"
                android:text="@string/activity_medal_ranking_proof_submit"
                android:textSize="11sp"
                android:textColor="@color/black"
                android:background="@drawable/custom_light_grey_background"
                android:gravity="center"
                android:clickable="true" />

            <!-- 注释按钮（屏幕最右边，与标题垂直对齐） -->
            <TextView
                android:id="@+id/helpButton"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:text="❓"
                android:textSize="16sp"
                android:textColor="@color/grey_60"
                android:gravity="center"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:clickable="true" />

            <!-- Title Text (Centered, Aligned with Help Button) -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_centerVertical="true"
                android:text="@string/activity_medal_ranking_medal_ranking"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="@color/black" />

        </RelativeLayout>

        <!-- 内容区域（添加下拉刷新） -->
        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipeRefreshLayout"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <!-- Loading Text -->
                <TextView
                    android:id="@+id/loadingText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerInParent="true"
                    android:text="@string/loading"
                    android:textSize="16sp"
                    android:textColor="@color/grey_60"
                    android:visibility="gone" />

                <!-- Error Text -->
                <TextView
                    android:id="@+id/errorText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerInParent="true"
                    android:text="@string/load_failed"
                    android:textSize="16sp"
                    android:textColor="@color/red_127"
                    android:visibility="gone" />

                <!-- Empty State Prompt -->
                <LinearLayout
                    android:id="@+id/emptyStateLayout"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerInParent="true"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="🏆"
                        android:textSize="48sp"
                        android:layout_marginBottom="16dp" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/activity_medal_ranking_no_ranking_data"
                        android:textSize="18sp"
                        android:textColor="@color/grey_60"
                        android:layout_marginBottom="8dp" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/activity_medal_ranking_submit_proof"
                        android:textSize="14sp"
                        android:textColor="@color/grey_60" />

                </LinearLayout>

                <!-- 排行榜列表 -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:padding="16dp"
                    android:visibility="gone" />

            </RelativeLayout>

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <!-- 底部导航按钮 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- My Button (Left) -->
            <TextView
                android:id="@+id/myCenterButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/activity_medal_ranking_my"
                android:textSize="16sp"
                android:textColor="@color/white"
                android:background="@drawable/custom_green_background"
                android:gravity="center"
                android:padding="16dp"
                android:clickable="true"
                android:layout_marginEnd="8dp" />

            <!-- Global Button (Right) -->
            <TextView
                android:id="@+id/globalStatsButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/activity_medal_ranking_global"
                android:textSize="16sp"
                android:textColor="@color/white"
                android:background="@drawable/custom_green_background"
                android:gravity="center"
                android:padding="16dp"
                android:clickable="true"
                android:layout_marginStart="8dp" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
